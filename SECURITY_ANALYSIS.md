# 🔒 Security Report

**Date:** 2026-07-21 | **Stack:** Unknown / Unknown

## Summary

Security posture is concerning with 2 high-severity container security issues: running as root user and overly permissive 777 file permissions. These fundamental violations of least privilege principles create significant attack surface. Immediate remediation of the Dockerfile is required to establish proper user isolation and file permissions.

## Severity Breakdown

| Severity | Total | Auto-Fixed |
|----------|-------|------------|
| 🟠 high | 2 | 2 |
| 🟡 medium | 4 | 4 |
| 🟢 low | 4 | 0 |

## Findings

### 🟠 High

**1. Running as Root User - Missing USER Directive** — `Dockerfile:1`
```
FROM alpine:3.24.1 AS alpine-base
```
The Dockerfile does not specify a non-root USER directive for the final runtime stages. Containers will run as root by default, which violates the principle of least privilege. ✅ Auto-fixed

**2. Overly Permissive File Permissions (chmod 777)** — `Dockerfile:130`
```
chmod -R 777 data/plugins-bundled
```
Setting permissions to 777 allows any user to read, write, and execute files in the plugins directory. This is overly permissive and violates the principle of least privilege. ✅ Auto-fixed

### 🟡 Medium

**1. Package Manager Cache Not Cleaned in Same Layer** — `Dockerfile:28`
```
RUN apk add --no-cache make build-base python3
```
While --no-cache is used for apk, the js-builder stage installs packages that may leave cache artifacts. More importantly, the go-builder stage's apk add doesn't use --no-cache. ✅ Auto-fixed

**2. Missing --no-cache Flag in Go Builder Stage** — `Dockerfile:62`
```
apk add --no-cache \
```
The go-builder stage conditionally runs apk add. While --no-cache is present, the conditional structure could be improved for clarity and the bash package installation increases attack surface. ✅ Auto-fixed

**3. Incomplete Dockerfile - Truncated Content** — `Dockerfile:138`
```
ARG GF_UID="
```
The Dockerfile appears to be truncated mid-definition, which could lead to build failures or unexpected behavior. The GF_UID ARG is not properly closed. ✅ Auto-fixed

**4. Using Mutable Base Image Tags** — `Dockerfile:17`
```
FROM --platform=${JS_PLATFORM} node:24-alpine AS js-builder-base
```
Using 'node:24-alpine' without a specific digest or patch version means the image can change between builds, potentially introducing vulnerabilities or breaking changes. ✅ Auto-fixed

### 🟢 Low

**1. Contains security-conscious comment about approvedGitRepositories - good practice noted** — `.yarnrc.yml`
Contains security-conscious comment about approvedGitRepositories - good practice noted ⚠️ Manual review

**2. Shell script with set -euo pipefail - good security practice** — `.citools/generate.sh`
Shell script with set -euo pipefail - good security practice ⚠️ Manual review

**3. Shell script with set -euo pipefail - good security practice** — `.citools/install.sh`
Shell script with set -euo pipefail - good security practice ⚠️ Manual review

**4. Validates input format before processing** — `.citools/install.sh`
Validates input format before processing ⚠️ Manual review

## Action Plan

*Generated by Agnixa Recon 2.0 Brain — 2026-07-21T16:02:07.763Z*
