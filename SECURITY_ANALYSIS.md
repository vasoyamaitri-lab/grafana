# 🔒 Security Report

**Date:** 2026-07-17 | **Stack:** Unknown / Unknown

## Summary

Security posture shows moderate risk with 4 medium-severity container security issues requiring attention. Primary concerns are root user execution in Docker build stages and overly permissive 777 file permissions that could enable privilege escalation or code injection. No critical vulnerabilities or auto-fixable issues were identified.

## Severity Breakdown

| Severity | Total | Auto-Fixed |
|----------|-------|------------|
| 🟡 medium | 4 | 4 |
| 🟢 low | 5 | 1 |

## Findings

### 🟡 Medium

**1. Running as root user in js-builder stage** — `Dockerfile:22`
```
FROM --platform=${JS_PLATFORM} ${JS_IMAGE} AS js-builder
```
The js-builder stage runs as root user by default, which violates the principle of least privilege and could allow container escape or privilege escalation if compromised. ✅ Auto-fixed

**2. Running as root user in go-builder stage** — `Dockerfile:55`
```
FROM ${GO_IMAGE} AS go-builder
```
The go-builder stage runs as root user by default, which violates the principle of least privilege. ✅ Auto-fixed

**3. Overly permissive file permissions (777)** — `Dockerfile:130`
```
chmod -R 777 data/plugins-bundled
```
Setting permissions to 777 allows any user to read, write, and execute files in the plugins directory, which could allow malicious code injection. ✅ Auto-fixed

**4. Potential arbitrary file copy via GRAFANA_TGZ argument** — `Dockerfile:105`
```
COPY ${GRAFANA_TGZ} /tmp/grafana.tar.gz
```
The GRAFANA_TGZ build argument allows specifying an arbitrary file path that gets copied into the container. Without validation, this could be exploited to include malicious files. ✅ Auto-fixed

### 🟢 Low

**1. Sets timezone via environment variable - not a security issue** — `jest.config.js`
Sets timezone via environment variable - not a security issue ⚠️ Manual review

**2. Contains eslint suppressions for type safety rules - may mask issues but not directly exploitable** — `eslint-suppressions.json`
Contains eslint suppressions for type safety rules - may mask issues but not directly exploitable ⚠️ Manual review

**3. Shell script with set -euo pipefail - good practice, no obvious injection points in visible portion** — `.citools/generate.sh`
Shell script with set -euo pipefail - good practice, no obvious injection points in visible portion ⚠️ Manual review

**4. Shell script accepts external input as argument - but validates format with @ check** — `.citools/install.sh`
Shell script accepts external input as argument - but validates format with @ check ⚠️ Manual review

**5. Go module cache mounted with default permissions** — `Dockerfile:79`
```
--mount=type=cache,target=/go/pkg/mod
```
The Go module cache is mounted without specifying uid/gid, which could lead to permission issues or security concerns in multi-user environments. ✅ Auto-fixed

## Action Plan

*Generated by Agnixa Recon 2.0 Brain — 2026-07-17T11:33:32.504Z*
