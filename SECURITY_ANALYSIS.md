# 🔒 Security Report

**Date:** 2026-07-21 | **Stack:** Unknown / Unknown

## Summary

Security posture requires immediate attention with 2 high-severity issues: overly permissive 777 file permissions in Dockerfile creating code injection risk, and an invalid Go version (1.26.5) that will cause build failures. Additionally, 3 medium-severity issues involve running containers as root user and unpinned Node.js image tags. The 5 low-severity findings are actually positive security practices already in place.

## Severity Breakdown

| Severity | Total | Auto-Fixed |
|----------|-------|------------|
| 🟠 high | 2 | 2 |
| 🟡 medium | 3 | 3 |
| 🟢 low | 5 | 1 |

## Findings

### 🟠 High

**1. Overly permissive file permissions (777)** — `Dockerfile:131`
```
chmod -R 777 data/plugins-bundled
```
Setting permissions to 777 allows any user to read, write, and execute files in the plugins directory. This is overly permissive and could allow malicious code injection. ✅ Auto-fixed

**2. Invalid Go version specified** — `Dockerfile:16`
```
FROM golang:1.26.5-alpine AS go-builder-base
```
Go version 1.26.5 does not exist (as of the knowledge cutoff). This appears to be a typo and should be a valid Go version like 1.23.5 or similar. ✅ Auto-fixed

### 🟡 Medium

**1. Running as root user in js-builder stage** — `Dockerfile:22`
```
FROM --platform=${JS_PLATFORM} ${JS_IMAGE} AS js-builder
```
The js-builder stage runs as root user by default, which violates the principle of least privilege and could allow container escape or privilege escalation if compromised. ✅ Auto-fixed

**2. Running as root user in go-builder stage** — `Dockerfile:56`
```
FROM ${GO_IMAGE} AS go-builder
```
The go-builder stage runs as root user by default, which violates the principle of least privilege. ✅ Auto-fixed

**3. Node.js image using major version tag** — `Dockerfile:17`
```
FROM --platform=${JS_PLATFORM} node:24-alpine AS js-builder-base
```
Using node:24-alpine without a specific patch version means the image can change unexpectedly, potentially introducing vulnerabilities or breaking changes. ✅ Auto-fixed

### 🟢 Low

**1. Contains security-conscious comment about approvedGitRepositories - good practice noted** — `.yarnrc.yml`
Contains security-conscious comment about approvedGitRepositories - good practice noted ⚠️ Manual review

**2. References environment variables for API tokens - good practice, credentials properly externalized** — `crowdin.yml`
References environment variables for API tokens - good practice, credentials properly externalized ⚠️ Manual review

**3. Shell script with set -euo pipefail - good practice for error handling** — `.citools/generate.sh`
Shell script with set -euo pipefail - good practice for error handling ⚠️ Manual review

**4. Shell script with proper error handling, validates input format** — `.citools/install.sh`
Shell script with proper error handling, validates input format ⚠️ Manual review

**5. Pinned but potentially vulnerable Alpine base image** — `Dockerfile:14`
```
FROM alpine:3.24.1 AS alpine-base
```
While the image is pinned to a specific version (good practice), Alpine 3.24.1 should be verified against known vulnerabilities and updated regularly. ✅ Auto-fixed

## Action Plan

*Generated by Agnixa Recon 2.0 Brain — 2026-07-21T16:26:04.053Z*
