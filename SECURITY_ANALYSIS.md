# 🔒 Security Report

**Date:** 2026-07-21 | **Stack:** Unknown / Unknown

## Summary

Security posture requires immediate attention with 2 high-severity container security issues: running as root user and an incomplete Dockerfile missing critical security configurations. The 3 medium-severity issues include overly permissive 777 file permissions, mutable image tags, and unvalidated build arguments. Positive findings include good shell scripting practices with proper error handling across CI tools.

## Severity Breakdown

| Severity | Total | Auto-Fixed |
|----------|-------|------------|
| 🟠 high | 2 | 2 |
| 🟡 medium | 3 | 3 |
| 🟢 low | 4 | 0 |

## Findings

### 🟠 High

**1. Running containers as root user** — `Dockerfile:1`
```
No USER directive specified in final stages
```
The Dockerfile does not specify a non-root USER in several build stages, which means containers will run as root by default. This is a security risk as it gives processes unnecessary privileges. ✅ Auto-fixed

**2. Incomplete Dockerfile - missing USER and final stage configuration** — `Dockerfile:136`
```
ARG GF_UID="
```
The Dockerfile appears to be truncated and incomplete. The distroless-prep stage is cut off, which may result in missing security configurations like proper USER setup. ✅ Auto-fixed

### 🟡 Medium

**1. Overly permissive file permissions** — `Dockerfile:131`
```
chmod -R 777 data/plugins-bundled
```
Setting permissions to 777 allows any user to read, write, and execute files in the plugins directory. This is overly permissive and violates the principle of least privilege. ✅ Auto-fixed

**2. Using mutable image tags** — `Dockerfile:14`
```
FROM alpine:3.24.1 AS alpine-base
```
While version tags are used, some images like node:24-alpine use major version tags that can change over time, leading to inconsistent builds and potential security issues. ✅ Auto-fixed

**3. Potential arbitrary file copy via GRAFANA_TGZ ARG** — `Dockerfile:100`
```
COPY ${GRAFANA_TGZ} /tmp/grafana.tar.gz
```
The GRAFANA_TGZ build argument is used directly in a COPY instruction without validation, which could allow copying unintended files if the argument is manipulated. ✅ Auto-fixed

### 🟢 Low

**1. Contains security-conscious comment about approvedGitRepositories - good practice documented** — `.yarnrc.yml`
Contains security-conscious comment about approvedGitRepositories - good practice documented ⚠️ Manual review

**2. Shell script uses set -euo pipefail which is good practice** — `.citools/generate.sh`
Shell script uses set -euo pipefail which is good practice ⚠️ Manual review

**3. Shell script uses set -euo pipefail which is good practice** — `.citools/install.sh`
Shell script uses set -euo pipefail which is good practice ⚠️ Manual review

**4. Validates input format before processing** — `.citools/install.sh`
Validates input format before processing ⚠️ Manual review

## Action Plan

*Generated by Agnixa Recon 2.0 Brain — 2026-07-21T16:12:49.002Z*
