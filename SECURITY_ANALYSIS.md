# 🔒 Security Report

**Date:** 2026-07-17 | **Stack:** Unknown / Unknown

## Summary

Security posture is concerning with 3 high-severity container security issues requiring immediate attention. Root user execution in Docker build stages and overly permissive 777 file permissions create significant privilege escalation and code injection risks. No auto-fixes were applied, requiring manual remediation of all 9 vulnerabilities.

## Severity Breakdown

| Severity | Total | Auto-Fixed |
|----------|-------|------------|
| 🟠 high | 3 | 3 |
| 🟡 medium | 1 | 1 |
| 🟢 low | 5 | 1 |

## Findings

### 🟠 High

**1. Running as Root User in js-builder Stage** — `Dockerfile:22`
```
FROM --platform=${JS_PLATFORM} ${JS_IMAGE} AS js-builder
```
The js-builder stage runs as root user by default, which violates the principle of least privilege and could allow container escape or privilege escalation attacks. ✅ Auto-fixed

**2. Running as Root User in go-builder Stage** — `Dockerfile:55`
```
FROM ${GO_IMAGE} AS go-builder
```
The go-builder stage runs as root user by default, which violates the principle of least privilege. ✅ Auto-fixed

**3. Overly Permissive File Permissions (777)** — `Dockerfile:135`
```
chmod -R 777 data/plugins-bundled
```
Setting permissions to 777 allows any user to read, write, and execute files in the plugins directory, which could allow malicious code injection. ✅ Auto-fixed

### 🟡 Medium

**1. Go Build Cache Mounted with Root Ownership** — `Dockerfile:79`
```
--mount=type=cache,target=/root/.cache/go-build
```
The Go build cache is mounted to /root/.cache which implies running as root and could lead to cache poisoning attacks. ✅ Auto-fixed

### 🟢 Low

**1. Contains security-conscious comment about approvedGitRepositories - good practice noted** — `.yarnrc.yml`
Contains security-conscious comment about approvedGitRepositories - good practice noted ⚠️ Manual review

**2. References environment variables for API tokens - properly configured, no hardcoded secrets** — `crowdin.yml`
References environment variables for API tokens - properly configured, no hardcoded secrets ⚠️ Manual review

**3. Shell script with set -euo pipefail - good practice, no obvious injection vulnerabilities in visible portion** — `.citools/generate.sh`
Shell script with set -euo pipefail - good practice, no obvious injection vulnerabilities in visible portion ⚠️ Manual review

**4. Shell script accepts external input for package installation - potential for command injection if input not validated** — `.citools/install.sh`
Shell script accepts external input for package installation - potential for command injection if input not validated ⚠️ Manual review

**5. Missing Health Check in Final Image** — `Dockerfile:110`
```
FROM alpine-base AS grafana-assets
```
The Dockerfile does not define a HEALTHCHECK instruction, making it harder to detect when the container is unhealthy. ✅ Auto-fixed

## Action Plan

*Generated by Agnixa Recon 2.0 Brain — 2026-07-17T10:51:11.102Z*
